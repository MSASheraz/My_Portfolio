# TODO - Admin Dashboard

- [x] Implement `resources/views/admin-dashboard/dashboard.blade.php` with your existing admin theme (purple gradients + dark/light CSS variables).
- [x] Add KPI cards + quick links + latest activity table, using live DB counts.
- [x] Add GSAP fade/slide-in animations and FOUC protection consistent with other admin pages.
- [ ] Sanity-check Blade syntax and run a quick render check (optional: `php artisan route:list`).


